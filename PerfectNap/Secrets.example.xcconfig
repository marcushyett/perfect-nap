// Template. Copy to Secrets.xcconfig (gitignored) and set the value to the push relay's
// RELAY_SHARED_SECRET (Vercel project "perfectnap-push" env var). Without it, cross-device push is
// simply disabled (RelayClient.isConfigured == false) — the app still works locally.
RELAY_SHARED_SECRET =
